import 'package:flutter/material.dart';
import 'app/app.dart'; // Import MyApp from its new location

void main() {
  runApp(const MyApp());
}

// MyApp and PropertyListScreen moved to separate files
